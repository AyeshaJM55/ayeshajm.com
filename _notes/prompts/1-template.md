pre-req:
- we need a tempalte to make this project
- there is a complete html clone of a project at `_notes/sequential/5-visune.io` which has a nice design.
- there is a skill `.agents/skills/fe-template-dev`` that tells how to make a nice template
- you may also refer to a template at `_notes/sequential/6-sample-template`

current task. only check details for existing rproject (visune.io) . read only. and share how you can make a tempalte from it. just thoughts for now. short resopnse expected.

---

Create a complete plan at `_notes/plans/1-template.md` that includes creation of a complete template of `visume` at `template/` dir.
this should be a compelte plan with phases, their milestones and complete plan's checklist.
remembe to make sure that a plan completes one piece of puzzle and includes testing so that it compeles and verifies one major task.
plan should justfiable phases, e.g., setup, components, etc.

start now and create this compelte detailed plan now.

---

looks like a great plan. start implementing it
go phase by phase in this manner:
- after implementing each phase, re-read next phase's details
- so taht you can refresh your memory.
start now. do not stop untill compelte plan is implemented.

---
