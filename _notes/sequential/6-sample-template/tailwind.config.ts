import preset from "@tpl/ui/preset";

// Root config re-exports the shippable preset so workspace tooling and
// downstream consumers share one token-aware Tailwind configuration.
export default preset;
